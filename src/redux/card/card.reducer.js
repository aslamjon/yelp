
const INTIAL_STATE = {
    sections: [
        {
            id: 1,
            title: '',
            img: '',
        }
    ]
}

const cardReducer = (state = INTIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}
export default cardReducer;